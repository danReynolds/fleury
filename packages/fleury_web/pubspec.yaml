name: fleury_web
description: >-
  Run fleury in a browser through the retained DOM host.
version: 0.0.0
publish_to: none

environment:
  sdk: ^3.10.4

dependencies:
  fleury:
    path: ../fleury
  web: ^1.1.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3
