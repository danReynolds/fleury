# Local development: resolve fleury from the monorepo. pub ignores this file
# when publishing, so no hand-edit is needed at release time.
dependency_overrides:
  fleury:
    path: ../fleury
  fleury_test:
    path: ../fleury_test
