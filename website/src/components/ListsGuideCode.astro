---
import DemoSource from './DemoSource.astro';
const sources = import.meta.glob<string>('../../examples/{lib,test}/lists/*.dart', {
  query: '?raw', import: 'default', eager: true,
});
interface Props { file: string; test?: boolean; focus?: string }
const { file, test = false, focus } = Astro.props;
const code = sources[`../../examples/${test ? 'test' : 'lib'}/lists/${file}`];
if (!code) throw new Error(`Lists guide file is missing: ${file}`);
---
<DemoSource code={code} focus={focus} />
