name: fleury_profiling
description: TUI profiling harness — capture (pty) + analyze (AnsiByteBreakdown) for fleury and peers. Not shipped.
publish_to: none
environment:
  sdk: ^3.6.0
dependencies:
  fleury:
    path: ../packages/fleury
